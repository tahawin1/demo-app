pipeline {
    agent any

    stages {
        stage('Test Git Checkout') {
            steps {
                git url: 'https://github.com/TON-UTILISATEUR/TON-REPO.git', branch: 'main'
            }
        }
    }
}
